apiVersion: v1
kind: Service
metadata:
  name: {{ include "hey.fullname" . }}
  labels:
    {{- include "hey.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hey.selectorLabels" . | nindent 4 }}
